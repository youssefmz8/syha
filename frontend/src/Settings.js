import React from 'react';

const Settings = () => {
    return (
        <div>
            <h2>Settings</h2>
            {/* Add settings management logic here */}
        </div>
    );
};

export default Settings;
